import "./style.css";
import ReactDOM from "react-dom/client";
import Overlay from "./ModelComponents/Overlay";
const root = ReactDOM.createRoot(document.querySelector("#root"));

root.render(
  <>
    <Overlay />
  </>
);
